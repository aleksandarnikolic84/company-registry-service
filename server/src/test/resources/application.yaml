spring:
  profiles:
    active: embedded
  # ===================================================================
  # Datasource
  # ===================================================================
  datasource:
    username: CompanyRegistry
    url: jdbc:tc:postgresql:16.6:///CompanyRegistry?TC_TMPFS=/testtmpfs:rw
    name:
    password:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false


